# Ứng Dụng Web Streamlit: Phân Loại Bằng Logistic Regression

Ứng dụng Streamlit này được tự động tạo và biên dịch từ Jupyter Notebook chứa luồng công việc Học máy (Machine Learning) sử dụng thuật toán **Logistic Regression**.

Mô hình được thiết kế để dự báo nhãn nhị phân (`PD`) dựa trên danh sách 24 biến độc lập phân nhóm (TC1..5, NL1..4, DK1..5, V1..6, TS1..4).

## 🚀 Cài Đặt

Môi trường khuyến nghị yêu cầu Python 3.8 trở lên. Vui lòng cài đặt các thư viện phụ thuộc bằng lệnh sau:

```bash
pip install -r requirements.txt
